---
title: teams.placements()
description: Get event placements for a team
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Cards, Card } from 'fumadocs-ui/components/card';
import { Info, ArrowRightLeft } from 'lucide-react';

## Signature

```python
import vlrdevapi as vlr

result = vlr.teams.placements(
    team_id: int,
    timeout: float = 5.0
) -> list[EventPlacement]
```

## Parameters

<TypeTable
  type={{
    team_id: {
      type: 'int',
      description: 'Team ID',
      required: true,
    },
    timeout: {
      type: 'float',
      description: 'Request timeout in seconds',
      default: '5.0',
    },
  }}
/>

## Return Value

**Type:** `list[EventPlacement]`

Returns a list of event placements.

<TypeTable
  type={{
    event_id: {
      type: 'int | None',
      description: 'Event ID',
    },
    event_name: {
      type: 'str | None',
      description: 'Event name',
    },
    event_url: {
      type: 'str | None',
      description: 'Event URL',
    },
    placements: {
      type: 'list[PlacementDetail]',
      description: 'Placement details (always a list, may be empty)',
      required: true,
    },
    year: {
      type: 'str | None',
      description: 'Year',
    },
  }}
/>

**PlacementDetail fields:**

<TypeTable
  type={{
    series: {
      type: 'str | None',
      description: 'Series name',
    },
    place: {
      type: 'str | None',
      description: 'Placement (e.g., "1st", "3rd-4th")',
    },
    prize_money: {
      type: 'str | None',
      description: 'Prize money (e.g., "$28,256")',
    },
  }}
/>

## Examples

### Get Event Placements

```python title="Get event placements"
import vlrdevapi as vlr

# Get all placements
placements = vlr.teams.placements(team_id=799)

print(f"Event placements: {len(placements)}")
for event in placements:
    print(f"\n{event.event_name} ({event.year})")
    for detail in event.placements:
        print(f"  {detail.series} - {detail.place}")
        if detail.prize_money:
            print(f"    Prize: {detail.prize_money}")
```

### Filter by Year

```python title="Filter by year"
import vlrdevapi as vlr

placements = vlr.teams.placements(team_id=799)

# Get 2024 placements
placements_2024 = [p for p in placements if p.year == "2024"]

print(f"2024 placements: {len(placements_2024)}")
for event in placements_2024:
    print(f"  {event.event_name}")
```


## Error Handling

- **Network failures**: Returns an empty list `[]`
- **Invalid team ID**: Returns an empty list `[]`
- **No placements**: Returns an empty list `[]`

The function never raises exceptions, making it safe to use without try-catch blocks.



## Related

<Cards className="mt-2">
  <Card
    href="/docs/api/teams/info"
    title="teams.info()"
    description="Get team information"
    icon={<Info className="h-5 w-5" />}
  />
  <Card
    href="/docs/api/teams/transactions"
    title="teams.transactions()"
    description="Get team transactions"
    icon={<ArrowRightLeft className="h-5 w-5" />}
  />
</Cards>

## Source

Data scraped from: `https://www.vlr.gg/team/{team_id}`
