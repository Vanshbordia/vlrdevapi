---
title: events.list_events()
description: List events with filters for tier, region, and status
icon: calendar
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Cards, Card } from 'fumadocs-ui/components/card';
import { Info, ListChecks, Trophy } from 'lucide-react';

## Signature

```python
import vlrdevapi as vlr

result = vlr.events.list_events(
    tier: EventTier | Literal["all", "vct", "vcl", "t3", "gc", "cg", "offseason"] = "all",
    region: str | None = None,
    status: EventStatus | Literal["all", "upcoming", "ongoing", "completed"] = "all",
    page: int = 1,
    limit: int | None = None,
    timeout: float = 5.0
) -> list[ListEvent]
```

## Parameters

<TypeTable
  type={{
    tier: {
      type: 'EventTier | str',
      typeDescription: 'EventTier enum or "all" | "vct" | "vcl" | "t3" | "gc" | "cg" | "offseason"',
      description: 'Event tier filter (use EventTier enum for autocomplete)',
      default: '"all"',
    },
    region: {
      type: 'str | None',
      description: 'Optional region filter (e.g., "Americas", "EMEA")',
      default: 'None',
    },
    status: {
      type: 'EventStatus | str',
      typeDescription: 'EventStatus enum or "all" | "upcoming" | "ongoing" | "completed"',
      description: 'Event status filter (use EventStatus enum for autocomplete)',
      default: '"all"',
    },
    page: {
      type: 'int',
      description: 'Page number for pagination (1-indexed)',
      default: '1',
    },
    limit: {
      type: 'int | None',
      description: 'Maximum number of events to return (None returns all events on the page)',
      default: 'None',
    },
    timeout: {
      type: 'float',
      description: 'Request timeout in seconds',
      default: '5.0',
    },
  }}
/>

## Return Value

**Type:** `list[ListEvent]`

Returns a list of event summaries. Each `ListEvent` object contains:

<TypeTable
  type={{
    id: {
      type: 'int',
      description: 'Event ID',
      required: true,
    },
    name: {
      type: 'str',
      description: 'Event name',
      required: true,
    },
    region: {
      type: 'str | None',
      description: 'Event region',
    },
    tier: {
      type: 'str | None',
      description: 'Event tier (e.g., "VCT", "VCL")',
    },
    start_date: {
      type: 'date | None',
      description: 'Event start date',
    },
    end_date: {
      type: 'date | None',
      description: 'Event end date',
    },
    start_text: {
      type: 'str | None',
      description: 'Human-readable start date',
    },
    end_text: {
      type: 'str | None',
      description: 'Human-readable end date',
    },
    prize: {
      type: 'str | None',
      description: 'Prize pool with currency formatting',
    },
    status: {
      type: 'str',
      typeDescription: '"upcoming" | "ongoing" | "completed"',
      description: 'Current event status',
      required: true,
    },
    url: {
      type: 'str',
      description: 'Full URL to event on VLR.gg',
      required: true,
    },
  }}
/>

## Examples

### List VCT Events

```python title="List VCT events"
import vlrdevapi as vlr
from vlrdevapi.events import EventTier, EventStatus

# Get ongoing VCT events
events = vlr.events.list_events(
    tier=EventTier.VCT,
    status=EventStatus.ONGOING,
    limit=10
)

for event in events:
    print(f"{event.name}")
    print(f"  Status: {event.status}")
    print(f"  Prize: {event.prize or 'TBD'}")
    print(f"  Region: {event.region or 'Global'}")
```

### Browse with Pagination

```python title="Browse with pagination"
import vlrdevapi as vlr

# Get page 2 of completed events
events = vlr.events.list_events(
    tier="gc",
    status="completed",
    page=2,
    limit=20
)

print(f"Found {len(events)} events on page 2")
for event in events:
    if event.start_date and event.end_date:
        duration = (event.end_date - event.start_date).days
        print(f"{event.name} - {duration} days")
```

### Find Events by Region

```python title="Find events by region"
import vlrdevapi as vlr

# Get all upcoming events in Americas
na_events = vlr.events.list_events(
    region="Americas",
    status="upcoming",
    limit=15
)

print(f"Americas events: {len(na_events)}")
for event in na_events[:5]:  # Show first 5
    print(f"• {event.name} ({event.tier})")
```

### Search by Tier String

```python title="Search by tier string"
import vlrdevapi as vlr

# Get all VCL events (using string instead of enum)
vcl_events = vlr.events.list_events(tier="vcl")

print(f"VCL events: {len(vcl_events)}")
for event in vcl_events:
    if event.prize:
        print(f"{event.name} - {event.prize}")
```

### Count Events by Status

```python title="Count events by status"
import vlrdevapi as vlr
from collections import Counter

# Get recent events
events = vlr.events.list_events(limit=100)

# Count by status
status_counts = Counter(event.status for event in events)
print("Event status distribution:")
for status, count in status_counts.items():
    print(f"  {status.title()}: {count}")
```

## Error Handling

- **Network failures**: Returns an empty list `[]`
- **Invalid parameters**: Uses defaults (e.g., unknown tier defaults to VCT)
- **No results**: Returns an empty list `[]`

The function never raises exceptions, making it safe to use without try-catch blocks.

## Tips

- Use `EventTier` and `EventStatus` enums for IDE autocomplete and type safety
- Combine `limit` with `page` for efficient pagination through large result sets
- Filter by `region` to narrow down results to specific geographic areas

## Related

<Cards className="mt-2">
  <Card
    href="/docs/api/events/info"
    title="events.info()"
    description="Get detailed event information"
    icon={<Info className="h-5 w-5" />}
  />
  <Card
    href="/docs/api/events/matches"
    title="events.matches()"
    description="Get matches for a specific event"
    icon={<ListChecks className="h-5 w-5" />}
  />
  <Card
    href="/docs/api/events/standings"
    title="events.standings()"
    description="Get event prize distribution and standings"
    icon={<Trophy className="h-5 w-5" />}
  />
</Cards>

## Source

Data scraped from: `https://www.vlr.gg/events`
